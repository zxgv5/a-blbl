# blbl-android

第三方哔哩哔哩安卓 App（初始版本骨架），对齐仓库根目录的 `BILI_THIRD_PARTY_ANDROID_APP_SPEC.md` 与 `example-pic/`。

## 构建

- `cd blbl-android`
- `./gradlew :app:assembleDebug`

## 运行

建议用 Android Studio 打开 `blbl-android/`，连接设备/模拟器运行 `app`。

